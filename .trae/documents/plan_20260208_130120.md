# 更新 AGENTS.MD 并记录后续重构计划

收到您的反馈，我将保留文档中关于 `/admin` 路由的描述，并将其作为后续代码重构的目标。

本次我将仅修正 `AGENTS.MD` 中那些**确认为文档过时**的部分：

## 1. 修正物理路径与项目结构
- **根目录**: 将文档中的 `src/` 统一更新为 `apps/web/src/`，以匹配 Monorepo 结构。
- **Route Groups**: 在目录树中补充 `(auth)` 文件夹，反映实际的代码组织结构（这不影响 URL）。
- **新增模块**: 在目录树中添加 `lobby` 页面和 `bgio-engine` 引擎目录。

## 2. 关于 /admin 路由的处理
- **保留现状**: 文档中关于 `/admin/users` 等路由的描述**保持不变**。
- **添加备注**: 我会在文档末尾或 TODO 列表中添加一条备注，说明当前代码位于 `(protected)` 下，后续需重构以匹配 `/admin` 路由路径。

## 3. 其他修正
- 更新 `Components` (添加 `ui`, `layout`) 和 `Hooks` 的实际位置描述。
- 添加新建的 `server-auth.tsx` 到上下文部分。

## 执行步骤
1.  修改 `AGENTS.MD` 更新文件树。
2.  创建一个新的 Todo 任务（在我的任务列表中），记录“重构代码以恢复 /admin 路由前缀”，以便后续处理。
